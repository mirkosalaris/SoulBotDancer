#ifndef FILTERS
#define FILTERS

float bassFilter(float sample);

float envelopeFilter(float sample);

float beatFilter(float sample);

float voiceFilter(float sample);

float envelopeFilter_highpass(float sample);

#endif
